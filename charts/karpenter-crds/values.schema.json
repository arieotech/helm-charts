{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "title": "Karpenter CRDs",
  "description": "Arieotech Karpenter CRDs chart values schema",
  "type": "object",
  "additionalProperties": false,
  "properties": {
    "keepCRDs": {
      "type": "boolean",
      "description": "Stamp helm.sh/resource-policy: keep on each CRD so `helm uninstall` does not delete the CRDs or the live custom resources they back. Set false only on ephemeral clusters."
    },
    "commonLabels": {
      "type": "object",
      "description": "Extra labels applied to all CRD resources.",
      "additionalProperties": { "type": "string" }
    },
    "commonAnnotations": {
      "type": "object",
      "description": "Extra annotations applied to all CRD resources.",
      "additionalProperties": { "type": "string" }
    }
  }
}
